<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.scoop.service.dao.GoalDAO">

    <insert id="insertgoal" parameterType="Goal">
        INSERT INTO Goal (key, wsid, gname,gstartdate,genddate, gstatus, uowner, gpublic, gfinish, gcreator)
        VALUES (key_seq.nextval, 1, #{gname},#{gstartdate},,#{genddate}, #{gstatus}, #{uowner}, #{gpublic}, #{gfinish},#{gcreator})
    </insert>


</mapper>